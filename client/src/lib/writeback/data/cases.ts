import type { Case } from "../types";

/**
 * Ten hosts, ten workloads, one question each.
 *
 * Every number in an option is produced by the model, not written by hand,
 * and the gate recomputes each of them a second way before the set ships.
 */
export const CASES: Case[] = [
  {
    slug: "the-ratio-is-not-of-installed-memory",
    name: "The reporting database",
    brief: "64 GiB in the machine, 48 of it held by the database's own heap.",
    setup: {
      host: "reports-01",
      ramGiB: 64,
      anonGiB: 48,
      backgroundRatio: 10,
      backgroundBytes: null,
      dirtyRatio: 20,
      dirtyBytes: null,
      writeMiBps: 40,
      deviceMiBps: 500,
      expireCentisecs: 3000,
      writebackCentisecs: 500,
    },
    question: "At how many MiB of dirty data do the flusher threads start?",
    options: [
      { id: "of-dirtyable", claim: "1638 MiB, ten percent of the 16 GiB that is dirtyable", says: { about: "background", mib: 1638 } },
      { id: "of-ram", claim: "6554 MiB, ten percent of the 64 GiB installed", says: { about: "nothing" } },
      { id: "hard-one", claim: "3277 MiB, because the flushers wait for dirty_ratio", says: { about: "background", mib: 3277 } },
      { id: "unbounded", claim: "There is no threshold until memory runs out", says: { about: "nothing" } },
    ],
    why:
      "global_dirtyable_memory() sums free pages and the file backed LRU. Anonymous memory is not in that sum, because a page of heap has nowhere to be written back to. The database's 48 GiB is therefore invisible to the ratio, and ten percent of what is left is 1638 MiB.",
    fix:
      "Read the threshold as a fraction of MemFree plus Cached rather than of MemTotal, and expect it to move as the workload's heap grows.",
    breaks: "the ratio is a percentage of installed memory",
  },
  {
    slug: "the-knob-you-raised-is-not-the-one-that-runs",
    name: "The tuning that changed nothing",
    brief: "Somebody tripled dirty_ratio to make the nightly export faster. The disk was never the problem.",
    setup: {
      host: "export-02",
      ramGiB: 32,
      anonGiB: 8,
      backgroundRatio: 10,
      backgroundBytes: null,
      dirtyRatio: 60,
      dirtyBytes: null,
      writeMiBps: 300,
      deviceMiBps: 800,
      expireCentisecs: 3000,
      writebackCentisecs: 500,
    },
    question: "Once the export is running steadily, how many MiB of dirty data is the machine holding?",
    options: [
      { id: "at-hard", claim: "14746 MiB, sixty percent of dirtyable, because that is the new ceiling", says: { about: "settled", mib: 14746 } },
      { id: "at-background", claim: "2458 MiB, ten percent of dirtyable, because that is where the flushers start", says: { about: "settled", mib: 2458 } },
      { id: "zero", claim: "Nothing: a device this fast never lets a page go dirty", says: { about: "settled", mib: 0 } },
      { id: "all-of-it", claim: "24576 MiB, all of dirtyable", says: { about: "nothing" } },
    ],
    why:
      "dirty_ratio is where the writer is made to wait, and the writer only gets there by outrunning the device. This one does not: 300 MiB a second against 800. The queue stops growing where the flushers are woken, which is dirty_background_ratio, and the tripled number is never consulted.",
    fix:
      "Tune dirty_background_ratio when you want to move where the queue sits. dirty_ratio is a backstop, and raising it only changes what happens on a machine that is already stalling.",
    breaks: "dirty_ratio is the number the queue settles at",
  },
  {
    slug: "thirty-five-seconds-of-nothing-happening",
    name: "The config file that was never written",
    brief: "A 12 MiB file, written and closed, on an otherwise idle host. The power went out nine seconds later.",
    setup: {
      host: "edge-03",
      ramGiB: 8,
      anonGiB: 2,
      backgroundRatio: 10,
      backgroundBytes: null,
      dirtyRatio: 20,
      dirtyBytes: null,
      writeMiBps: 0,
      deviceMiBps: 500,
      expireCentisecs: 3000,
      writebackCentisecs: 500,
    },
    question: "How long could that file have stayed in memory before a flusher took it?",
    options: [
      { id: "five", claim: "5 seconds, the flusher wake interval", says: { about: "nothing" } },
      { id: "thirty", claim: "30 seconds, dirty_expire_centisecs", says: { about: "nothing" } },
      { id: "thirty-five", claim: "35 seconds: the expiry, and then the next wake", says: { about: "age", seconds: 35 } },
      { id: "immediate", claim: "It was on disk when close() returned", says: { about: "nothing" } },
    ],
    why:
      "A page is not eligible for writeback until it is dirty_expire_centisecs old, and a flusher only looks every dirty_writeback_centisecs. Thirty seconds of ineligibility and then up to five more waiting to be noticed. Measured on an idle host: 64 MiB sat at 64 MiB of Dirty for thirty seconds without a byte moving, and reached zero at thirty five.",
    fix:
      "close() is not a durability barrier and neither is the default flusher interval. Anything that has to survive the machine needs fsync, and the cost of that call is the whole point of it.",
    breaks: "a closed file reaches the disk within one flusher interval",
  },
  {
    slug: "the-knob-that-reads-zero",
    name: "The sysctl.d file in the wrong order",
    brief: "The file sets dirty_background_ratio and then dirty_background_bytes. Reading back the ratio shows 0.",
    setup: {
      host: "build-04",
      ramGiB: 16,
      anonGiB: 4,
      backgroundRatio: null,
      backgroundBytes: 268435456,
      dirtyRatio: 20,
      dirtyBytes: null,
      writeMiBps: 100,
      deviceMiBps: 600,
      expireCentisecs: 3000,
      writebackCentisecs: 500,
    },
    question: "Which knob is the kernel actually using for the background threshold?",
    options: [
      { id: "ratio", claim: "dirty_background_ratio, because a ratio outranks an absolute", says: { about: "nothing" } },
      { id: "stricter", claim: "Whichever of the two works out smaller", says: { about: "nothing" } },
      { id: "neither", claim: "Neither: a zero in either one disables the background threshold", says: { about: "nothing" } },
      { id: "bytes", claim: "dirty_background_bytes, because it was written last", says: { about: "live-knob", name: "dirty_background_bytes" } },
    ],
    why:
      "The two forms of each knob are one setting with two faces. Writing either zeroes the other, last write wins, and the loser reads back as 0. Measured: dirty_ratio 20 with dirty_bytes 0, write 104857600 to dirty_bytes and the ratio reads 0, write 20 to the ratio and the bytes read 0.",
    fix:
      "Set one form of each pair and never both. The zero you read in the other is not an unsupported knob, it is the kernel telling you which one lost.",
    breaks: "setting both forms applies the stricter of the two",
  },
  {
    slug: "the-gap-is-what-stalls-you",
    name: "The log shipper on a spinning disk",
    brief: "200 MiB a second of compressed logs onto a device that retires 120.",
    setup: {
      host: "logship-05",
      ramGiB: 16,
      anonGiB: 6,
      backgroundRatio: 10,
      backgroundBytes: null,
      dirtyRatio: 20,
      dirtyBytes: null,
      writeMiBps: 200,
      deviceMiBps: 120,
      expireCentisecs: 3000,
      writebackCentisecs: 500,
    },
    question: "Does this workload reach dirty_ratio and get throttled?",
    options: [
      { id: "yes", claim: "Yes: it writes 80 MiB a second more than the device retires, so the queue only grows", says: { about: "throttled", value: true } },
      { id: "no-small", claim: "No: 200 MiB a second is far below the 2048 MiB threshold", says: { about: "nothing" } },
      { id: "no-flushers", claim: "No: the flushers start at the background threshold and keep it there", says: { about: "throttled", value: false } },
      { id: "only-bursts", claim: "Only if the logs arrive in bursts", says: { about: "nothing" } },
    ],
    why:
      "Once the flushers are running the device is retiring 120 MiB a second and the workload is adding 200. The difference does not go anywhere. Dirty climbs from the background threshold to the hard one at 80 MiB a second and the writer is then held to the device's rate.",
    fix:
      "The throttle is set by the gap between the write rate and the device, not by how large either is. A workload that outruns its disk stalls at some point no matter where the thresholds are.",
    breaks: "throttling depends on how much you write rather than on the gap",
  },
  {
    slug: "raising-the-ceiling-buys-seconds",
    name: "The ceiling raised to stop the stalls",
    brief: "Same shipper, same disk. dirty_ratio went from 20 to 50 to stop the pauses.",
    setup: {
      host: "logship-06",
      ramGiB: 16,
      anonGiB: 6,
      backgroundRatio: 10,
      backgroundBytes: null,
      dirtyRatio: 50,
      dirtyBytes: null,
      writeMiBps: 200,
      deviceMiBps: 120,
      expireCentisecs: 3000,
      writebackCentisecs: 500,
    },
    question: "How many MiB of unwritten data does the host now hold once it settles?",
    options: [
      { id: "same", claim: "1024 MiB, the background threshold, as before", says: { about: "settled", mib: 1024 } },
      { id: "hard", claim: "5120 MiB, the new hard threshold, and all of it is unwritten", says: { about: "settled", mib: 5120 } },
      { id: "less", claim: "Less than before, because the flushers now have more room to work", says: { about: "nothing" } },
      { id: "none", claim: "None: raising the ceiling let the device catch up", says: { about: "nothing" } },
    ],
    why:
      "The device still retires 120 MiB a second and the workload still asks for 200. Raising the ceiling did not change either. What it changed is how far the queue grows before the writer is stopped, which is how much data is sitting in volatile memory when the machine loses power: five gibibytes instead of two.",
    fix:
      "A higher dirty_ratio does not buy throughput on a device that is already saturated. It buys a longer run between stalls, paid for in data you have not written down.",
    breaks: "a higher dirty_ratio makes a saturated device faster",
  },
  {
    slug: "two-identical-machines",
    name: "The pair that behaved differently",
    brief: "Same hardware, same sysctl.d, same 128 GiB. One runs a 96 GiB JVM heap.",
    setup: {
      host: "app-07",
      ramGiB: 128,
      anonGiB: 96,
      backgroundRatio: 5,
      backgroundBytes: null,
      dirtyRatio: 10,
      dirtyBytes: null,
      writeMiBps: 80,
      deviceMiBps: 900,
      expireCentisecs: 3000,
      writebackCentisecs: 500,
    },
    question: "How much memory on this one is dirtyable?",
    options: [
      { id: "all", claim: "131072 MiB: dirtyable memory is the memory in the machine", says: { about: "nothing" } },
      { id: "heap", claim: "98304 MiB: the heap is memory like any other", says: { about: "nothing" } },
      { id: "remainder", claim: "32768 MiB: what is left once the heap is out of the sum", says: { about: "dirtyable", mib: 32768 } },
      { id: "threshold", claim: "1638 MiB: dirtyable memory is the background threshold", says: { about: "dirtyable", mib: 1638 } },
    ],
    why:
      "Anonymous memory is not in global_dirtyable_memory(), so the heap subtracts from the base the ratio is taken against. Two machines with identical sysctls and identical RAM have thresholds four times apart because one of them is using its memory for something that cannot be written back.",
    fix:
      "When a threshold has to be a fixed number of bytes across a fleet, set dirty_background_bytes. A ratio deliberately tracks a moving base.",
    breaks: "identical sysctls on identical machines mean identical byte thresholds",
  },
  {
    slug: "thresholds-set-backwards",
    name: "The pair that crossed over",
    brief: "Someone set dirty_background_ratio to 40 and dirty_ratio to 10. The kernel took both.",
    setup: {
      host: "misconfig-08",
      ramGiB: 32,
      anonGiB: 12,
      backgroundRatio: 40,
      backgroundBytes: null,
      dirtyRatio: 10,
      dirtyBytes: null,
      writeMiBps: 150,
      deviceMiBps: 700,
      expireCentisecs: 3000,
      writebackCentisecs: 500,
    },
    question: "Where does dirty data top out on this host?",
    options: [
      { id: "background", claim: "8192 MiB, the background threshold, since that is what the flushers watch", says: { about: "settled", mib: 8192 } },
      { id: "rejected", claim: "Nowhere: the kernel refuses a background threshold above the hard one", says: { about: "nothing" } },
      { id: "average", claim: "5120 MiB, midway between the two", says: { about: "nothing" } },
      { id: "hard", claim: "2048 MiB, the hard threshold, which the writer now meets first", says: { about: "settled", mib: 2048 } },
    ],
    why:
      "Neither write is rejected, and the ordering is not checked. The background wake sits above the hard limit, so the writer reaches the hard limit first and is throttled there, on a machine whose device could have kept up easily. The symptom is a stall nobody can explain from a sysctl listing that looks generous.",
    fix:
      "Keep the background threshold below the hard one. Read both back after any change, because nothing will tell you they crossed.",
    breaks: "the kernel validates that the background threshold is below the hard one",
  },
  {
    slug: "a-fast-device-never-reaches-the-wall",
    name: "The NVMe and the nervous ceiling",
    brief: "dirty_ratio pinned at 3 to limit data loss. The device retires 2 GiB a second.",
    setup: {
      host: "nvme-09",
      ramGiB: 64,
      anonGiB: 16,
      backgroundRatio: 1,
      backgroundBytes: null,
      dirtyRatio: 3,
      dirtyBytes: null,
      writeMiBps: 600,
      deviceMiBps: 2048,
      expireCentisecs: 3000,
      writebackCentisecs: 500,
    },
    question: "Does a ceiling this low make the writer stall?",
    options: [
      { id: "no", claim: "No: the device retires more than the workload writes, so the queue never grows past the flusher wake", says: { about: "throttled", value: false } },
      { id: "yes-low", claim: "Yes: three percent is far too little headroom", says: { about: "throttled", value: true } },
      { id: "yes-rate", claim: "Yes: 600 MiB a second fills 1475 MiB in under three seconds", says: { about: "nothing" } },
      { id: "sometimes", claim: "Only when the file being written is larger than the threshold", says: { about: "nothing" } },
    ],
    why:
      "Filling the gap between the two thresholds requires writing faster than the device empties it. At 600 against 2048 there is no gap to fill: the flushers wake at one percent and take pages away faster than the workload makes them. A low ceiling on a fast device costs nothing and caps what is lost on a power cut.",
    fix:
      "Low thresholds are cheap when the device is quick. The thing that makes them expensive is a slow device, and that is worth measuring before assuming.",
    breaks: "a low dirty_ratio always costs throughput",
  },
  {
    slug: "what-the-power-cut-takes",
    name: "The rack that lost a feed",
    brief: "Mid write, both thresholds at their defaults, a device that cannot keep up.",
    setup: {
      host: "batch-10",
      ramGiB: 24,
      anonGiB: 4,
      backgroundRatio: 10,
      backgroundBytes: null,
      dirtyRatio: 20,
      dirtyBytes: null,
      writeMiBps: 400,
      deviceMiBps: 150,
      expireCentisecs: 3000,
      writebackCentisecs: 500,
    },
    question: "How many MiB of acknowledged writes were never on the disk?",
    options: [
      { id: "interval", claim: "750 MiB, five seconds of writes, the flusher interval", says: { about: "nothing" } },
      { id: "settled", claim: "4096 MiB: the queue was full to the hard threshold", says: { about: "settled", mib: 4096 } },
      { id: "background", claim: "2048 MiB, the background threshold, which is where writeback keeps it", says: { about: "settled", mib: 2048 } },
      { id: "nothing-lost", claim: "None: write() does not return until the data is durable", says: { about: "nothing" } },
    ],
    why:
      "The workload outran the device, so the queue grew to dirty_ratio and stayed there. Everything between the disk and the hard threshold had been acknowledged to the application and existed only in volatile memory. The hard threshold is not just a stall point, it is the size of the hole.",
    fix:
      "Size the thresholds by what you can afford to lose, not only by the stalls you want to avoid. Data that must survive needs fsync, and the applications that skip it are the ones that lose most here.",
    breaks: "the page cache can lose at most one flusher interval of writes",
  },
];
