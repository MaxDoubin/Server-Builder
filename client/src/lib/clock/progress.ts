/** Cases diagnosed, per browser. Same caveats as everywhere else on the site. */

const KEY = "maxdoubin-clock-solved";

export const loadSolvedClocks = (): string[] => {
  try {
    const raw = localStorage.getItem(KEY);
    if (!raw) return [];
    const parsed: unknown = JSON.parse(raw);
    return Array.isArray(parsed) && parsed.every((x) => typeof x === "string") ? parsed : [];
  } catch {
    return [];
  }
};

export function recordSolvedClock(slug: string): void {
  const list = loadSolvedClocks();
  if (list.includes(slug)) return;
  try {
    localStorage.setItem(KEY, JSON.stringify([...list, slug]));
  } catch {
    // Private browsing or a full quota.
  }
}
